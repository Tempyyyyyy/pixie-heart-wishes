import { Server } from "lucide-react";
import { PageStub } from "@/components/launcher/PageStub";
const Servers = () => <PageStub icon={Server} title="Серверы" description="Подключайся к топовым серверам Minecraft в один клик." />;
export default Servers;
